apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-html
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>GitOps Mini App</title>
    </head>
    <body>
      <h1>{{ .Values.message }}</h1>
      <p>Deployed with Helm and Argo CD</p>
    </body>
    </html>
